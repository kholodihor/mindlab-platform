import { BlockText } from "@/components/LoginForm/BlockText.tsx";
import { BlockForm } from "@/components/LoginForm/BlockForm.tsx";

export const LoginForm = () => {
  return (
    <>
      <BlockText />
      <BlockForm />
    </>
  )
}